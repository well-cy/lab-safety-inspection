# E4 第一轮 lab_coat 定向增训 — 数据准备中期报告（暂停待 B1）

日期：2026-09-06
状态：**已暂停**。B3 已下载并完成全部审计；**B1（自采集约150张）尚未提供**，按约定不伪造、不替代，等待用户提供后再合成最终增训集并开始训练。

---

## 1. B3 数据集审计结果（ez-spn/lab-coat-v8qin）

### 1.1 基本信息
| 项目 | 值 |
|---|---|
| 来源 | Roboflow Universe `ez-spn/lab-coat-v8qin` |
| 许可证 | CC BY 4.0（已核实项目页 API license 字段） |
| 项目标称 | 479 张（train 373 / valid 87 / test 19） |
| 实际落盘 | 476 张（3 张因跨 split 同名被覆盖/合并，见 1.4） |
| 标注 | polygon（外接矩形已转换），原生 3 类 |

### 1.2 类别构成
| 类别 | 框数 | 含该类图片数 | 映射到本项目 |
|---|---|---|---|
| Lab_coat | 480 | 450 | lab_coat (2) |
| Gloves | 101 | — | gloves (1) |
| Goggles | 8 | — | goggles (3) |
| （空标注背景图） | — | 23 | 保留为负样本 |

### 1.3 去重审计（红线全部通过）
| 检查项 | 结果 | 判定 |
|---|---|---|
| **B3 ↔ dataset2 (external_test) 近重复** | **0 对**（文件名/MD5/SHA256/pHash≤10 全查） | ✅ **无泄漏，红线通过** |
| B3 ↔ dataset1 近重复 | 4 对（涉及 B3 侧 5 张，其中 LC7.jpg 与 ds1_valid 视频帧汉明距离 8） | ⚠️ 需剔除 B3 侧 5 张 |
| B3 内部完全重复 | 1 对（valid/LC12.jpg ≡ test/LC12.jpg） | 剔除 1 张 |
| B3 内部近重复 | 609 对（视频帧系列为主，如 1726038964-xxx 连拍） | 与 dataset1 内部情况类似，保留但注意有效多样性 |
| B3 跨 split 同名 | 3 组（download.jpeg、LC7、LC12） | 集成时按 MD5 去重 |

**B3 部分与 LAB_PPE(dataset1) 同源的怀疑被证实**：4 对近重复中 2 对是 crop_margin 系列直接命中 dataset1 train 图片。但由于泄漏方向是"B3 含 dataset1 已有图"，剔除后不影响 dataset1 的独立性，**dataset1 test 无任何污染**（近重复对中无 ds1_test）。

### 1.4 版权风险（新发现，需决策）
B3 混入 **20 张 gettyimages/istockphoto 图库水印图**（train 16 + valid 4）。这与当初否决 B2 的理由相同。**建议剔除这 20 张**，与 B2 的处理保持一致（待确认）。

### 1.5 来源构成（476 张）
| 来源系列 | 张数 | 占比 | 域价值 |
|---|---|---|---|
| RF 用户自采白大褂（detect_2026xxx_白大褂/普通外套） | 132 | 28% | ★★★ 正是缺的真实白大褂域 |
| NN_NNN 系列 | 104 | 22% | 真实场景居多 |
| crop_margin 系列 | 90 | 19% | 与 dataset1 部分同源 |
| 其他（日期时间命名等真实照片） | 76 | 16% | 真实场景 |
| 10位数字-数字系列 | 36 | 8% | 连拍系列 |
| 图库水印图 | 20 | 4% | 建议剔除 |
| LC 系列 | 18 | 4% | 混合 |

### 1.6 标注抽检
12 张可视化抽检图已生成至 `outputs/b3_label_check/`（b3check_01~12，覆盖自采/crop/连拍/frame/图库各系列）。框坐标由 polygon 外接矩形转换，抽检文件名与 Lab_coat 语义相符。建议人工快速复核。

---

## 2. B3 可用数量结论

| 处理 | 张数 |
|---|---|
| B3 落盘 | 476 |
| − 与 dataset1 近重复（5 张） | 471 |
| − 图库水印图（20 张，待确认） | 451 |
| − 内部完全重复（1 张） | **450** |
| 其中含 Lab_coat 框 | 约 **425–435 张** |

## 3. 当前缺少什么（暂停原因）

**B1 自采集数据（目标约 150 张真实实验室白大褂照片）尚未提供。**

请提供后放入任意目录并告知路径（要求建议：≥5 人、≥3 种光照、近/中/远 3 种距离、穿脱两种状态更佳）。我将：
1. 对 B1 做同样的标注格式约定（YOLO bbox，类别 id=2 lab_coat）——**B1 需要人工标注**，或告知由我生成预标注供人工校正
2. B1 与 dataset1/dataset2/B3 做同样的去重审计
3. 输出最终增训数据清单（数量/来源/类别框数/90-10 split），确认后按方案Ⅰ训练（best.pt 续训，epochs=30，lr0=0.001，imgsz=640，batch=16）

## 4. 待用户决策项
1. **B1 提供方式**：图片路径 + 标注方式（人工标注 or 我预标注+人工复核）
2. **B3 的 20 张图库水印图**：建议剔除（与 B2 处理一致）
3. B3 与 dataset1 近重复的 5 张：建议剔除（默认执行，除非反对）

---

## 附录：执行记录
- 下载脚本：`tools/download_b3.py`（REST search API + 单图 API，断点续传；该项目 API 响应结构与 dataset2 不同：`image.urls.original` + `annotation.boxes`，polygon 的 x/y/w/h 即外接矩形）
- 审计脚本：`tools/dedup_b3.py`（报告：`data/b3_dedup_report.txt`，明细：`data/b3_dedup_pairs.csv`）
- 抽检脚本：`tools/visualize_b3.py`（输出：`outputs/b3_label_check/`）
- dataset2 全程零接触：下载与审计均为只读比对，未参与任何训练/验证
